Options -Indexes
RewriteEngine On

# Le dice a Apache que la base para las URLs es esta carpeta.
RewriteBase /ofertapp-app/adm-ofertapp/

# ====================================================================
# REGLAS DE EXCEPCIÓN
# ====================================================================

# Excepción para la carpeta de la API: si la URL empieza con 'api-ofertapp/', no hagas nada y déjala pasar.
# (Esta regla es por si la API estuviera aquí dentro, la dejamos por seguridad)
RewriteRule ^api-ofertapp/ - [L]

# ¡NUEVA EXCEPCIÓN CRUCIAL!
# Si la URL empieza con 'vistas/', no hagas nada y déjala pasar.
# Esto permitirá que tus archivos CSS, JS e imágenes se carguen correctamente.
RewriteRule ^vistas/ - [L]


# ====================================================================
# REGLAS DE RUTA PRINCIPAL
# Si la petición no fue para la API o para 'vistas', y no es un archivo o carpeta real...
# ====================================================================
RewriteCond %{REQUEST_FILENAME} !-f
RewriteCond %{REQUEST_FILENAME} !-d

# ...entonces, reenvía la solicitud a index.php para que la maneje la plantilla.
RewriteRule ^(.*)$ index.php?ruta=$1 [QSA,L]